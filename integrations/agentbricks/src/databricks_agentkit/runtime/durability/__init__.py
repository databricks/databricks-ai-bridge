"""Compatibility package for :mod:`databricks_agentbricks.runtime.durability`.

Leaf modules in this package alias their Agent Bricks implementations while the package
itself remains local so imports preserve one module identity for shared runtime classes.
"""
